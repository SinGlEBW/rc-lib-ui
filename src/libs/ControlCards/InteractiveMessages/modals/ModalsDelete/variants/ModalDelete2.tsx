import React, { FC, ReactNode } from "react"

export interface ModalDelete2Props {
  children?: ReactNode;
}

const ModalDelete2Memo:FC<ModalDelete2Props> = (props) => {
  return (
    <>
      
    </>
  )
};

export const ModalDelete2 = React.memo(ModalDelete2Memo);
